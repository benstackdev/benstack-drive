services:
  db:
    image: postgres:18-alpine
    ports:
      - "5432:5432"
    env_file:
      - ./.env
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    volumes:
      - benstack-drive:/var/lib/postgresql

volumes:
  benstack-drive:
    name: "benstack-drive"
